{{- if and .Values.webhook.enabled .Values.certRotation.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "renovate-operator.resourceName" (dict "suffix" "webhook-server-cert" "context" $) }}
  namespace: {{ .Release.Namespace }}
{{- end }}
