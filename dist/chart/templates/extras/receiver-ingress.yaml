{{- if and .Values.manager.enabled .Values.receiver.enabled .Values.receiver.ingress.enabled }}
{{- $component := "receiver" }}
{{- $fullname := include "renovate-operator.resourceName" (dict "suffix" $component "context" $) }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullname }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "renovate-operator.labels" $ | nindent 4 }}
    app.kubernetes.io/component: {{ $component }}
  {{- with .Values.receiver.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- include "renovate-operator.ingressSpec" (dict "ingress" .Values.receiver.ingress "svcPort" (int .Values.receiver.service.port) "fullname" $fullname) | nindent 2 }}
{{- end }}
