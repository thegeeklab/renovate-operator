receiver:
  enabled: true
  gateway:
    enabled: true
    className: nginx
    listeners:
      - name: http
        port: 80
        protocol: HTTP
      - name: https
        port: 443
        protocol: HTTPS
        hostname: receiver.example.com
        tls:
          mode: Terminate
          certificateRefs:
            - name: receiver-tls
              kind: Secret
