receiver:
  enabled: true
  gateway:
    enabled: true
    className: nginx
    hosts:
      - renovate-receiver.example.com
    filters:
      - type: RequestHeaderModifier
        requestHeaderModifier:
          set:
            - name: X-Forwarded-Proto
              value: https
