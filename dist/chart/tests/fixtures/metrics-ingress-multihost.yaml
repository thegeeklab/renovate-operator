metrics:
  ingress:
    enabled: true
    hosts:
      - host: a.example.com
        paths:
          - path: /a
            pathType: Prefix
      - host: b.example.com
        paths:
          - path: /b
            pathType: ImplementationSpecific
