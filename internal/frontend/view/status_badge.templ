package view

import (
	"context"

	"github.com/thegeeklab/renovate-operator/internal/frontend/viewmodel"
)

templ statusBadge(ctx context.Context, status viewmodel.Status) {
	<span class={ status.BadgeClass() }>{ status.TranslatedLabel(ctx) }</span>
}
